def countVowels (s):
    for character in s:
        if character == ('a'):
             count += 1
        elif character == ('e'):
             count += 1
        elif character == ('i'):
             count += 1
        elif character == ('o'):
             count += 1
        elif character == ('u'):
             count += 1
    print ('Number of vowels:', count)
